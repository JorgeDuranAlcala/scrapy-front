import { AccountsAdvancedFilterQuery } from '../_types'

const accountsDefaultFilterValues: AccountsAdvancedFilterQuery = {
  field: 'businessName',
  condition: 'contains',
  input: ''
}

export default accountsDefaultFilterValues
